export type Rect = { x: number; y: number; h: number; w: number };
export type Point = { x: number; y: number };
export type ChessFile = "a" | "b" | "c" | "d" | "e" | "f" | "g" | "h";
export type FileAndRank = { file: ChessFile; rank: number };
